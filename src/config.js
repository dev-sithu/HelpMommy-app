const config = {
    "storePrefix": "hm_",
    "userStoreKey": "hm_user",
    "settingStoreKey": "hm_config",
    "toastOptions": {
        position: "top-center",
        theme: "dark",
        autoClose: 1200,
    }
};

export default config;
